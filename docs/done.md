# Toki — Completed Tasks

This file records tasks that have been completed and removed from [`backlog.md`](./backlog.md).

## Phase 1 — Foundation

**Goal:** A working CLI that parses W3C DTCG tokens, resolves references, and generates plain CSS + JS output. ✅ **Complete.**

| #    | Task                                                                                                                                                                             | Type  | Est. | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- | ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1.1  | Scaffold repository: `pnpm init`, `tsconfig.json` (strict), `tsup.config.ts`, `vitest.config.ts`, `.oxlintrc.json`, `.nvmrc` (24), `engines` field in `package.json`             | Setup | 1h   | All scaffolding files in place. `package.json` has `engines` (`>=24.0.0`), `packageManager` (`pnpm@10.32.1`). Strict `tsconfig.json` (with `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`). `.nvmrc` = `24`. `tsup`, `vitest`, `oxlint`, `prettier` configs present. README scaffold written.                                                                                                                                                                |
| 1.2  | Define core TypeScript types: `TokenGroup`, `DesignToken`, `ResolvedToken`, `DesignTokenDocument`, `TokenType`, `OutputArtifact`                                                 | Types | 2h   | `src/core/types.ts` — discriminated `TokenGroupNode`/`DesignToken` (discriminated via `kind`), `TokenNode`, `TokenTree`, `ResolvedToken`, `OutputFormat`, `OutputArtifact`, `Generator`, `GeneratorOptions`, plus `TOKEN_TYPES`/`ALL_FORMATS` runtime sets.                                                                                                                                                                                                            |
| 1.3  | Build JSON parser: read file → validate against expected structure → produce normalized `TokenTree`                                                                              | Core  | 3h   | `src/core/parser.ts` — `readTokenFile`, `parseTokenJson`, `parseTokenDocument`, `parseTokenTree`. Validates top-level object, reserved `$` keys, `$type` enum, no `$value`+children mixing, non-object children, non-object `$extensions`. Preserves JSON insertion order; emits flat `DesignToken[]` in document order. Each token carries an `id` (dotted path).                                                                                                     |
| 1.4  | Build reference resolver: expand `{group.token}` references → concrete values. Build dependency graph, topological sort, circular dependency detection with clear error messages | Core  | 4h   | `src/core/resolver.ts` — DFS-coloured topological sort with cycle detection (`CircularReferenceError` listing the cycle), missing-reference detection (`MissingReferenceError`), pure-reference (native-type-preserving) substitution, embedded-reference string interpolation, and recursive substitution into composite objects/arrays.                                                                                                                              |
| 1.5  | Build `$type` inheritance: group-level `$type` propagates to child tokens unless overridden                                                                                      | Core  | 2h   | Resolver's `collectLeaves` walks the tree carrying an inherited-type context; a group's `$type` overrides the context, a token's explicit `$type` wins over the context. Missing-type → `TokenTypeError`.                                                                                                                                                                                                                                                              |
| 1.6  | Build CSS generator: resolved tokens → `:root { --color-primary: #1a73e8; }` with deterministic key ordering and header comment                                                  | Gen   | 3h   | `src/generators/css.ts` — `:root` block, `--kebab-case` custom properties, `/* Generated by toki v{version} — do not edit */` header, deterministic document order. Handles color, dimension, fontFamily, fontWeight, duration, number, lineHeight, letterSpacing, `cubicBezier` (→ `cubic-bezier(...)`), `shadow` (single + multi). Multi-property composites (typography/border/transition) are skipped (deferred to Phase 2 transformers). Writes `css/tokens.css`. |
| 1.7  | Build JS generator: resolved tokens → `export const colorPrimary = "#1a73e8";` (camelCase naming) + `.d.ts` type declarations file                                               | Gen   | 2h   | `src/generators/js.ts` — `export const camelCase = <literal>;` named exports + `export declare const camelCase: <type>;` declarations. Type inference (`inferJsType`) for primitives, tuples, and `readonly` object shapes with sorted keys. Reserved-word escaping (`default_`), leading-digit prefix (`_500Value`), and `GeneratorError` on identifier collisions. Writes `js/tokens.js` + `js/tokens.d.ts`.                                                         |
| 1.8  | Build CLI entry point: `toki build --input tokens.json --output ./dist --format css,js` using Commander.js                                                                       | CLI   | 2h   | `src/cli.ts` (Commander.js), `src/core/pipeline.ts` (orchestrator), `src/utils/writer.ts` (deterministic disk writes; `--clean` only removes the platform subdirectories being written, never arbitrary user files). `src/version.ts` reads `package.json` version at runtime. Comma- or space-separated `--format`; stable `TokiError`-based error reporting with non-zero exit. Auto-invokes only as the main entry (safe to import as a library).                   |
| 1.9  | Write Vitest unit tests: parser (3–5 cases), resolver (refs, nested refs, circular deps, missing refs), CSS generator, JS generator                                              | Test  | 4h   | 6 test files, 59 tests: `src/core/parser.test.ts` (11), `src/core/resolver.test.ts` (16), `src/generators/css.test.ts` (10), `src/generators/js.test.ts` (12), `src/core/pipeline.test.ts` (5 — end-to-end via writer with a tmp dir), `src/utils/naming.test.ts` (5). All green.                                                                                                                                                                                      |
| 1.10 | Write README.md: installation, quick start, input format reference, output examples for CSS and JS                                                                               | Docs  | 2h   | `README.md` updated: Status → Phase 1 complete; expanded quick start with output tree and error examples; added a concrete "Output examples" section showing the generated `tokens.css`, `tokens.js`, and `tokens.d.ts` from a sample input; platform table annotated with Phase-2 markers.                                                                                                                                                                            |

### Phase 1 Exit Criteria — all satisfied

- [x] `toki build --input tokens.json --output ./dist --format css,js` produces correct, deterministic output
- [x] All tests pass (`pnpm test`) — 59/59
- [x] Zero TypeScript errors (`pnpm typecheck`)
- [x] README has copy-paste quick start
- [x] Output files have `/* Generated by toki v{version} — do not edit */` header

## Phase 2 — Multi-Platform

**Goal:** All six target platforms plus React/Next.js generate correct, idiomatic output from the same input. ✅ **Complete.**

| #   | Task                                                                                                                       | Type | Est. | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --- | -------------------------------------------------------------------------------------------------------------------------- | ---- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2.1 | Build value transformer registry: `ColorTransformer`, `DimensionTransformer`, `FontWeightTransformer`, `ShadowTransformer` | Core | 3h   | `src/core/transformer.ts` — type-keyed registry (`registerTransformer`/`getTransformer`/`transformTokens`) applied as a real pipeline stage per format (`Parse → Resolve → Transform → Generate`). `ColorTransformer` normalizes hex for all platforms (shorthand expansion, lowercase). `DimensionTransformer` (`"8px"`→`8`, `"1.5rem"`→`24` @ base 16), `FontWeightTransformer` (`"bold"`→`"700"`), `ShadowTransformer` (DTCG shadow → `{ shadowColor, shadowOffset, shadowOpacity, shadowRadius, elevation }`), plus `FontFamilyTransformer` (first family name) and `TypographyTransformer` (RN composite fields) — all RN-only. |
| 2.2 | Build React Native generator                                                                                               | Gen  | 3h   | `src/generators/react-native.ts` — `tokens.js` grouped by pluralized category (`colors`, `spacing`, …) with nesting preserved + `styles.js` with `StyleSheet.create()` groups (`backgrounds`, `textColors`, `textStyles`). Shared grouping/serialization in `src/utils/grouping.ts`.                                                                                                                                                                                                                                                                                                                                                 |
| 2.3 | Build Angular latest generator                                                                                             | Gen  | 3h   | `src/generators/angular.ts` — `_tokens.scss` (`$kebab-case` vars), `tokens.scss` (`@use "./tokens" as tokens;` + `:root` custom properties), `tokens.ts` (`CONSTANT_CASE`), `tokens.module.ts` (`InjectionToken<DesignTokens>` + value + provider). Name-collision detection per namespace.                                                                                                                                                                                                                                                                                                                                          |
| 2.4 | Build Angular 11 legacy generator                                                                                          | Gen  | 2h   | `src/generators/angular-11.ts` — reuses the Angular renderers; `tokens.scss` uses `@import "tokens";` only (no `@use`/`@forward`); no `InjectionToken` module.                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2.5 | Build Svelte generator                                                                                                     | Gen  | 2h   | `src/generators/svelte.ts` — `tokens.css` (`:root` custom properties shared with the CSS generator via `renderCssCustomProperties`) + `tokens.ts` camelCase ES module. Works with scoped `<style>` via normal CSS-var cascade.                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2.6 | Build React / Next.js generator                                                                                            | Gen  | 3h   | `src/generators/react.ts` — `theme.ts` nested category object exported `as const` + `Theme` type + default export (CSS-in-JS / Tailwind `theme.extend` compatible) + companion `tokens.css` for `next-themes`.                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2.7 | Multi-format single run                                                                                                    | CLI  | 2h   | All 7 formats registered in `src/generators/index.ts`; `--format all` expands to every format (`ALL_FORMATS_KEYWORD`); duplicate formats deduplicated; outputs under per-platform subdirectories.                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2.8 | Per-platform README in each output subdirectory                                                                            | Docs | 2h   | `src/generators/readme.ts` — every generator (all 7) emits a `README.md` artifact: quick start, naming convention, multi-theme note, known limitations.                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2.9 | Vitest tests for new generators + transformers                                                                             | Test | 5h   | 101 new tests (160 total): `transformer.test.ts` (37 — incl. seeded-PRNG property tests: any valid hex → valid RN color, px/rem → numbers, 100–900 weights, shadow invariants), 5 generator suites, `grouping.test.ts`, `readme.test.ts`. 16 snapshots (one+ per platform) in `src/**/__snapshots__`. Added `@vitest/coverage-v8@^3` dev dep; coverage 93.7% stmts / 88.1% branch / 92.3% funcs / 93.7% lines (thresholds 90/80/85/90).                                                                                                                                                                                              |

### Phase 2 Exit Criteria — all satisfied

- [x] All 7 generators (css, js, react-native, angular, angular-11, svelte, react) produce correct output
- [x] `toki build --format all` generates the complete output tree (23 artifacts for the sample set)
- [x] Angular latest output uses `@use`; Angular 11 output uses `@import` only
- [x] React Native output uses raw numbers for dimensions (not `px` strings)
- [x] React theme object is nested by category (colors, spacing, typography)
- [x] All tests pass, snapshot tests capture output regressions — 160/160
- [x] Each output subdirectory has a README

## Phase 3 — Configuration & Multi-Theme

**Goal:** Production-grade ergonomics. Config file support, multi-theme output, naming transforms, and plugin hooks. ✅ **Complete.**

| #   | Task                                                                                                                                                                                                                | Type  | Est. | Notes                                                                                                                                                                                                                                                                                                                                                                                                                |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- | ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 3.1 | Build config file loader: `toki.config.ts` (or `.js`) loaded via `jiti`. Config discovery: current directory → `--config` flag → default                                                                            | Infra | 3h   | `src/core/config.ts` — `discoverConfig` searches CWD for `toki.config.ts` → `.js` → `.mjs`. `loadConfigFile` uses jiti for TS/JS loading. `loadConfig` combines discovery + loading. `mergeConfig` overlays CLI flags onto config values. Config discovery order: `--config` flag → `toki.config.ts` → `toki.config.js` → `toki.config.mjs`.                                                                         |
| 3.2 | Define config schema: `input` (string or array), `output` (per-format directory), `themes` (mapping of theme name → token file), `naming` (per-format naming convention), `transforms` (custom transform functions) | Types | 2h   | `src/core/types.ts` — `TokiConfig` interface, `NamingConvention` type, `TransformPlugin` type, `TransformContext` interface. `GeneratorOptions` extended with `theme` and `naming` fields. `validateConfig` in `config.ts` validates all fields with clear error messages. `ConfigError` class added to `errors.ts`.                                                                                                 |
| 3.3 | Multi-theme support: config specifies `{ light: "tokens/light.json", dark: "tokens/dark.json" }` → generators produce separate output files per theme (e.g., `tokens.light.css`, `tokens.dark.css`)                 | Core  | 4h   | `themePath` helper in `format.ts` inserts theme suffix before file extension. All 7 generators use `themePath` when `options.theme` is set. Pipeline's `BuildOptions` and `GenerateOptions` accept `theme`. CLI iterates themes when config has `themes` map. `--theme` flag builds a single theme. README files are not themed.                                                                                     |
| 3.4 | Naming transform system: `camelCase` (JS default), `kebab-case` (CSS default), `CONSTANT_CASE` (Angular/TS), `SCREAMING_SNAKE_CASE` (alias). Configurable per platform in config file                               | Core  | 2h   | `getNamingFunction` in `naming.ts` maps convention string to conversion function. `toScreamingSnakeCase` alias added. All generators accept `options.naming` and use `getNamingFunction` instead of hardcoded naming. `makeIdentifier` accepts naming parameter. Angular's `deriveAngularNames` accepts TS naming convention.                                                                                        |
| 3.5 | Custom transform plugin API: users can register transform functions in config that modify token values before generation. Example: `transforms: [addAlphaChannel, convertToRem]`                                    | Core  | 4h   | `applyCustomTransforms` in `pipeline.ts` executes plugins in registration order after built-in platform transforms. `TransformPlugin` type: `(token, context) => ResolvedToken`. Config `transforms` array threaded through `BuildOptions` → `GenerateOptions` → pipeline. Each transform receives `TransformContext` with `platform`.                                                                               |
| 3.6 | `toki init` command: scaffolding wizard that generates a starter `tokens.json` (with sample color, spacing, typography tokens) + `toki.config.ts`                                                                   | CLI   | 2h   | `init` command in `cli.ts` writes sample `tokens.json` (color, spacing, typography tokens) and `toki.config.ts` (TypeScript with `TokiConfig` type). `--dir` flag specifies scaffold directory. Conflict detection: skips existing files with message. Prints next steps.                                                                                                                                            |
| 3.7 | Verbose/debug mode: `--verbose` flag prints resolution trace (which tokens were resolved, in what order), generator output paths, and timing                                                                        | CLI   | 1h   | `ResolveOptions` with `trace` callback in `resolver.ts`. Pipeline threads trace through to resolver. CLI `--verbose` logs config discovery, per-theme processing, token resolution trace (collection count, order, resolved values), and per-build timing via `performance.now()`.                                                                                                                                   |
| 3.8 | Write Vitest tests: config loading (TS and JS), multi-theme output, naming transforms for all 4 conventions, plugin execution order                                                                                 | Test  | 4h   | 55 new tests (215 total): `config.test.ts` (39 — discovery, loading, validation, merging), `format.test.ts` (5 — themePath), extended `pipeline.test.ts` (+6 — theme, transforms, verbose), extended `naming.test.ts` (+5 — getNamingFunction, SCREAMING_SNAKE_CASE). All green.                                                                                                                                     |
| 3.9 | Update README: config file reference, multi-theme examples, plugin API documentation                                                                                                                                | Docs  | 2h   | README updated: Status → Phase 3 complete. Added sections for Configuration File (schema table), Multi-Theme Support (config example + output tree), Naming Conventions (table + override example), Custom Transform Plugins (function signature + example), Verbose Mode (flag docs + output example). Updated CLI flags table with `--config`, `--theme`, `--verbose`. Updated architecture tree with `config.ts`. |

### Phase 3 Exit Criteria — all satisfied

- [x] `toki build` with no flags discovers and loads `toki.config.ts`
- [x] Multi-theme config produces separate output files per theme
- [x] Naming transforms produce correct output for each convention
- [x] Custom transform functions execute in registration order
- [x] `toki init` scaffolds a working starter project
- [x] All tests pass — 215/215
- [x] README documents config file schema completely

## Phase 4 (partial) — Watch, Diff, Import

**Goal:** Watch mode, diff tooling, and import from Style Dictionary + Figma Tokens Studio formats. ✅ **Complete (4 of 8 tasks).**

| #   | Task                                                                                                                                                                                                                                                          | Type   | Est. | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 4.1 | `toki watch` command: file system watcher with 200ms debounce, incremental rebuild, change summary with timestamps, graceful error handling (continues watching on build failure), SIGINT/SIGTERM cleanup                                                     | CLI    | 3h   | `src/core/watch.ts` — uses `chokidar` (added as runtime dependency). `startWatch()` returns a cleanup function. Resolves build config from CLI options + config file. Watches all theme files + config file. Initial build on start, then debounced rebuilds on change. `src/core/watch.test.ts` (5 tests) — startWatch exports, cleanup function, initial build output, multi-theme configs.                                                                                                                                 |
| 4.2 | `toki diff <old> <new>` command: set-diff of resolved tokens by dotted path, reports added/removed/changed with before/after values, `--json` output format                                                                                                   | CLI    | 4h   | `src/core/diff.ts` — `runDiff()` reads both files, resolves tokens, computes diff by id. `diffTokens()` compares token maps. Deep value equality check for composites. `formatDiffTerminal()` renders human-readable output with `+`/`-`/`~` prefixes. `formatDiffJson()` returns flat entry array. `src/core/diff.test.ts` (20 tests) — identical/added/removed/changed/mixed, composite equality, array values, terminal formatting, JSON format, integration with file I/O.                                                |
| 4.3 | `toki import --from style-dictionary --input <path>`: reads SD v3 format, converts `value`→`$value`, `type`→`$type`, `comment`→`$description`, preserves `$extensions` and reference syntax, validates output as W3C DTCG                                     | Import | 4h   | `src/importers/style-dictionary.ts` — `convertStyleDictionary()` recursive converter. Handles group-level `$type`, `$extensions`, and `comment`/`description` mapping. Validates token types against DTCG spec. `importStyleDictionary()` reads file, converts, writes output (defaults to `tokens.json` in input directory). `src/importers/style-dictionary.test.ts` (17 tests) — conversion rules, group-level type, nested groups, references, extensions, validation, DTCG output validation, integration with file I/O. |
| 4.4 | `toki import --from figma-tokens --input <path>`: reads Figma Tokens Studio export, strips `$metadata`/`$themes`, strips theme selector top-level keys, converts `value`→`$value`, `type`→`$type`, `description`→`$description`, merges multiple theme groups | Import | 3h   | `src/importers/figma-tokens.ts` — `convertFigmaTokens()` strips META_KEYS, converts remaining top-level groups, merges into one DTCG root. Handles group-level `$type` and `description`. `importFigmaTokens()` reads file, converts, writes output. `src/importers/figma-tokens.test.ts` (18 tests) — theme key stripping, metadata/themes stripping, description mapping, nested groups, references, multiple theme merge, group-level type, validation, DTCG output validation, integration.                               |

### Infrastructure changes (shared)

- Added `chokidar@^5.0.0` as runtime dependency
- Added `IMPORT_ERROR` error code + `ImportError` class to `src/utils/errors.ts`
- Added `parseFormats()` to `src/generators/index.ts` (extracted from CLI to avoid circular deps)
- Registered `diff`, `watch`, `import` commands in `src/cli.ts`
- Updated barrel export `src/index.ts` with new modules

### Phase 4 (partial) Exit Criteria — all satisfied

- [x] `toki watch` rebuilds on file change with 200ms debounce
- [x] `toki diff` reports all token changes accurately (added, removed, changed)
- [x] Style Dictionary and Figma Tokens Studio imports produce valid W3C DTCG output
- [x] 274 tests pass (59 new across diff, watch, SD import, Figma import)
- [x] Zero TypeScript errors, zero lint warnings
- [x] Build succeeds (tsup + type generation)

## Phase 4 (complete) — Polish & Ecosystem

**Goal:** JSON Schema publication, CI integration docs, performance benchmarks, npm publish preparation. ✅ **Complete.**

| #   | Task                                                                                                                                     | Type  | Est. | Notes                                                                                                                                                                                                                                                           |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------- | ----- | ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 4.5 | JSON Schema publication: generate and publish `schema/toki-input.json` — IDE autocomplete and VS Code validation. Referenced from README | Infra | 2h   | `schema/toki-input.json` covers the full W3C DTCG spec: 13 token types, recursive group/token definitions, optional `$description`/`$extensions`. Includes `$schema` reference and `$id`. Referenced in README under a new "JSON Schema" section.               |
| 4.6 | CI integration documentation                                                                                                             | Docs  | 0h   | Skipped — user opted out of CI/CD workflows.                                                                                                                                                                                                                    |
| 4.7 | Performance benchmarking: Vitest benchmark suite — 500/1000/5000 tokens across all 7 generators                                          | Test  | 2h   | `src/core/benchmark.test.ts` (9 tests) — generates synthetic token sets (color + dimension) with 5-iteration warmup and p95 percentile assertions. Thresholds: parse/resolve <100ms (≤1k) / <200ms (5k), generate <500ms (≤1k) / <2000ms (5k). 281 total tests. |
| 4.8 | npm publish preparation: `package.json` `bin`/`files` verified, `.npmignore`, `prepublishOnly` script, `CHANGELOG.md`                    | Infra | 2h   | `bin.toki` and `files` (dist, schema) already present. `prepublishOnly` runs clean → typecheck → lint → test → build. `.npmignore` excludes src, docs, tool configs. `CHANGELOG.md` follows Keep a Changelog with all features listed under 0.1.0.              |

### Phase 4 Exit Criteria — all satisfied

- [x] JSON Schema is valid and referenced in README
- [ ] CI example workflows — skipped (user opted out of CI/CD)
- [x] Benchmark shows <2000ms for 5000 tokens across 7 platforms (p95)
- [x] `npm publish` is possible (private registry or public when ready)
- [x] 281 tests pass (7 new benchmark tests)
- [x] Zero TypeScript errors, zero lint warnings
- [x] Build succeeds (tsup + type generation)

## Phase 5 — Public Release

**Goal:** npm publication, community docs, and security hardening. ✅ **Complete.**

| #   | Task                                                            | Type    | Est. | Notes                                                                                                                      |
| --- | --------------------------------------------------------------- | ------- | ---- | -------------------------------------------------------------------------------------------------------------------------- |
| 5.1 | Add `repository`, `homepage`, `bugs` fields to `package.json`   | Infra   | 0.5h | Added `repository` (git URL), `homepage` (GitHub README), and `bugs` (GitHub Issues) to package.json.                      |
| 5.2 | Add `"sideEffects": false` to `package.json`                    | Infra   | 0.1h | Set `sideEffects: false` for tree-shaking compatibility.                                                                   |
| 5.3 | Create `CONTRIBUTING.md` with setup, testing, and PR guidelines | Docs    | 1h   | `CONTRIBUTING.md` covers setup, dev workflow (test, lint, format), code conventions, adding generators, and PR guidelines. |
| 5.4 | Create `CODE_OF_CONDUCT.md` (Contributor Covenant)              | Docs    | 0.5h | `CODE_OF_CONDUCT.md` — Contributor Covenant v2.1 with project maintainer contact.                                          |
| 5.5 | Add `npm pack` verification (check published files)             | Infra   | 1h   | Added `npm pack --dry-run` step to CI (later removed when CI was dropped). Can be verified manually before publish.        |
| 5.6 | Add security policy (`SECURITY.md`) + `npm audit`               | Infra   | 1h   | `SECURITY.md` created with supported versions and reporting process. `npm audit` can be run manually.                      |
| 5.7 | Publish v0.1.0 to npm                                           | Release | 0.5h | Published to npm registry as `toki`. Binaries available via `npx toki` or `npm install -g toki`.                           |

### Phase 5 Exit Criteria — all satisfied

- [x] `package.json` has complete metadata (repository, homepage, bugs, sideEffects)
- [x] CONTRIBUTING.md, CODE_OF_CONDUCT.md, SECURITY.md published
- [ ] CI verification — skipped (user opted out of CI/CD; `npm pack --dry-run` and `npm audit` can be run manually)
- [x] Package published to npm registry
- [x] All 282 tests pass
- [x] Zero TypeScript errors, zero lint warnings
- [x] Build succeeds (tsup + type generation)

## Phase 6 — Post-v1 Platforms

**Goal:** Extend toki to additional framework targets beyond the original seven platforms. ✅ **Complete.**

| #   | Task                                                                                                                                                                                                                                                                    | Type | Est. | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 6.1 | **StencilJS generator** — new 8th platform: `:root` CSS custom properties + `tokens.ts` (camelCase exports + grouped `tokens` object) + `tokens.d.ts` + `types.ts` (per-category union types + full `TokenName` union for type-safe `@Prop()` decorators) + `README.md` | Gen  | 6h   | `src/generators/stencil.ts` — reuses `renderCssCustomProperties` from the CSS generator for `tokens.css`. `renderTokensModule` emits camelCase named exports with collision detection (`GeneratorError`) plus a grouped `tokens` object keyed by raw first path segment and exported `as const`, with a companion `TokenCategory` type. `renderUnionTypes` emits `${PascalCase}Token` unions per category and a `TokenName` union combining them. Registered as `'stencil'` in `OutputFormat`, `ALL_FORMATS`, the generator registry (`src/generators/index.ts`), `DEFAULT_NAMING` (`src/core/config.ts`), and the `"all"` format expansion. 13 new tests in `src/generators/stencil.test.ts` (294 total). Released in v1.1.0. |

## Future Backlog (partial) — P5, P7

**Goal:** Post-v1 performance & DX enhancements. P5 and P7 complete; remaining items (P6, P8–P10) stay in [`backlog.md`](./backlog.md).

| #   | Task                                                                                                                | Type | Est. | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --- | ------------------------------------------------------------------------------------------------------------------- | ---- | ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P5  | **Parallel Generator Execution** — run selected generators concurrently via `Promise.all` after Transform stage     | Perf | 1h   | `Generator.generate` is now async (`Promise<readonly OutputArtifact[]>`); all 10 built-in generators converted. `generate()`/`generateFromDocument()` in `src/core/pipeline.ts` are async and dispatch each selected format's Transform → Generate work through `Promise.all`, then sort artifacts by `relativePath` so output stays deterministic regardless of completion order. Failures in any generator reject the whole run. Callers updated: CLI, watch, all 3 MCP tools, benchmark (async `runWithTiming`), and every generator/pipeline test (`expect(...).rejects.toThrow` for collision cases). 2 new pipeline tests cover the async contract and rejection propagation. 474 tests pass; typecheck/lint/build clean.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| P7  | **Incremental Builds (Cache Layer)** — SHA-256 cache of resolved token trees, skip re-generation on unchanged input | Perf | 6h   | `src/utils/hashing.ts` (`sha256`, `canonicalJson`) + `src/core/cache.ts` (`CacheStore` — versioned `.toki/cache.json`, atomic temp+rename writes, tolerant load, LRU cap of 200 entries, `serializeResolvedTokens`, `allPathsExist`). `runPipeline` checks a **two-tier key**: tier 1 = raw input bytes hash (hit ⇒ skips parse/resolve/generate/write, ~0ms rebuilds), tier 2 = resolved-token-tree hash (hit ⇒ skips transform/generate/write). Both keys include formats, naming, theme, `TOKI_VERSION`, and a config-file hash (proxy for `transforms`). Hits verify the previously written artifact paths still exist on disk and regenerate if missing. `BuildResult.cached` signals hits; CLI prints `No changes — N tokens up to date (cached)` and skips the writer; watch prints `Up to date`; MCP `build_tokens` now runs through `runPipeline` and reports `cached`. New `cache?: boolean` config field (default `true`) + `--no-cache` CLI flag; `readTokenFileRaw` added to the parser for byte-accurate hashing. 12 new tests in `src/core/cache.test.ts` (hit/miss matrix, tier-2 whitespace hit, deleted-output regeneration, corrupt/version-mismatched file tolerance, LRU cap, atomicity). 492 tests pass; typecheck/lint/build clean. |

## Future Backlog (partial) — P9, P10

**Goal:** Post-v1 editor/DX enhancements. P9 and P10 complete; remaining items (P6, P8) stay in [`backlog.md`](./backlog.md).

| #   | Task                                                                                                                            | Type | Est. | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --- | ------------------------------------------------------------------------------------------------------------------------------- | ---- | ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P9  | **Output Schema Publication** — publish JSON Schema per platform output format for IDE autocomplete on generated files          | Docs | 3h   | `src/schemas/output.ts` — `buildOutputSchema`/`buildOutputSchemas`/`generateOutputSchemas` emit draft-07 JSON Schemas describing each platform's JSON view of the generated artifacts from the resolved token set (platform transforms applied, mirroring each generator): CSS/Tailwind custom-property maps (`--color-primary`), JS-family named exports (`colorPrimary`, `COLOR_PRIMARY` for Angular), grouped categories for React Native/React, and camelCase exports + grouped `tokens` object for Stencil. Every schema lists all emitted keys in `properties` with `additionalProperties: false`, sorted `required`, per-token descriptions, and value typing (`string`/`number`/`boolean`/nested objects with sorted keys). Composite expansion reuses `expandCompositeToken` (CSS) and a new exported `tailwindCompositeDeclarations` helper (refactored out of the Tailwind renderer — no behavior change, pinned by its snapshot tests). New `toki schema` CLI command (`--input`/`--output` default `./schema/output`/`--format` default `all`/`--config`/`--theme`/`--verbose`), honoring multi-theme configs and per-format naming overrides. 22 new tests in `src/schemas/output.test.ts` (envelope, per-format property/type assertions, collision errors, determinism, file I/O + theme suffixes). 533 tests pass; typecheck/lint/build clean. |
| P10 | **IntelliSense Snippet Generation** — editor-agnostic completion spec (LSP / VS Code) from resolved tokens for design system DX | DX   | 4h   | `src/intellisense/` — `spec.ts` (editor-agnostic `CompletionSpec`: per-token id, type, resolved value, description, `cssVariable` `--kebab-case`, camelCase `identifier`, formatted `detail`), `vscode.ts` (`.code-snippets` document: one snippet per token keyed by dotted id, dotted/kebab/camel prefixes, body `var(--color-primary)`), `lsp.ts` (JSON-serializable `CompletionItem[]`: kind Value (6), plain-text insert format, markdown documentation, multi-form `filterText`, zero-padded `sortText`), `generate.ts` (`writeCompletionFiles` I/O), and a barrel `index.ts`. New `toki completions` CLI command (`--input`/`--output` default `./completions`/`--editor spec\|vscode\|lsp\|all`/`--config`/`--theme`/`--verbose`) writing `spec.json`, `tokens.code-snippets`, and `tokens.lsp.json`, with theme-suffixed filenames per config themes. 19 new tests across `spec`/`vscode`/`lsp`/`generate` suites (entry derivation, prefix/body shape, LSP fields, file I/O, determinism). 533 tests pass; typecheck/lint/build clean.                                                                                                                                                                                                                                                                                                                |

## Future Backlog (partial) — P18

**Goal:** Keep the toolchain current — majors behind at the time were Vitest 3 → 4 and Commander 13 → 15. P18 complete; remaining items (P6, P8, P11–P17) stay in [`backlog.md`](./backlog.md).

| #   | Task                                                                                                                                 | Type  | Est. | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| --- | ------------------------------------------------------------------------------------------------------------------------------------ | ----- | ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P18 | **Dependency upgrade pass** — Vitest 4, oxlint 1.76, Commander 15, jiti 2.7, prettier 3.9, MCP SDK 1.30; unify TS 7 in `build:types` | Infra | 2-3h | Upgraded: `vitest`/`@vitest/coverage-v8` 3.2.7 → **4.1.10**, `oxlint` 1.75 → **1.76.0**, `commander` 13.1 → **15.0.0**, `jiti` 2.4.2 → **2.7.0**, `prettier` 3.5.3 → **3.9.6**, `@modelcontextprotocol/sdk` 1.29 → **1.30.0**, `tsup` → 8.5.1. `build:types` now pins `typescript@7.0.2` (was 6.0.3), matching the devDependency. **Vitest 4 migration:** `coverage` moved to the config top level (canonical location in v4); the v8 provider counts ~5pp more branch points (same code: 83.31% on v3 vs 78.08% on v4), so the branch threshold was recalibrated 80 → 75 with the rationale documented in `vitest.config.ts`. `pnpm bench` fixed — v4 bench mode only matches `*.bench.*` globs, so the script now runs the timing suite directly (`vitest run src/core/benchmark.test.ts`). Prettier 3.6+ default formatting changes applied across 25 files (`pnpm format`). Verified: 617 tests pass, bench 9/9 (p95 thresholds hold), typecheck/lint/build clean, coverage gates pass, `toki build` smoke-tested via the built CLI. |
