# Toki — Completed Tasks

This file records tasks that have been completed and removed from [`backlog.md`](./backlog.md).

## Phase 1 — Foundation

**Goal:** A working CLI that parses W3C DTCG tokens, resolves references, and generates plain CSS + JS output. ✅ **Complete.**

| # | Task | Type | Est. | Notes |
|---|---|---|---|---|
| 1.1 | Scaffold repository: `pnpm init`, `tsconfig.json` (strict), `tsup.config.ts`, `vitest.config.ts`, `.oxlintrc.json`, `.nvmrc` (24), `engines` field in `package.json` | Setup | 1h | All scaffolding files in place. `package.json` has `engines` (`>=24.0.0`), `packageManager` (`pnpm@10.32.1`). Strict `tsconfig.json` (with `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`). `.nvmrc` = `24`. `tsup`, `vitest`, `oxlint`, `prettier` configs present. README scaffold written. |
| 1.2 | Define core TypeScript types: `TokenGroup`, `DesignToken`, `ResolvedToken`, `DesignTokenDocument`, `TokenType`, `OutputArtifact` | Types | 2h | `src/core/types.ts` — discriminated `TokenGroupNode`/`DesignToken` (discriminated via `kind`), `TokenNode`, `TokenTree`, `ResolvedToken`, `OutputFormat`, `OutputArtifact`, `Generator`, `GeneratorOptions`, plus `TOKEN_TYPES`/`ALL_FORMATS` runtime sets. |
| 1.3 | Build JSON parser: read file → validate against expected structure → produce normalized `TokenTree` | Core | 3h | `src/core/parser.ts` — `readTokenFile`, `parseTokenJson`, `parseTokenDocument`, `parseTokenTree`. Validates top-level object, reserved `$` keys, `$type` enum, no `$value`+children mixing, non-object children, non-object `$extensions`. Preserves JSON insertion order; emits flat `DesignToken[]` in document order. Each token carries an `id` (dotted path). |
| 1.4 | Build reference resolver: expand `{group.token}` references → concrete values. Build dependency graph, topological sort, circular dependency detection with clear error messages | Core | 4h | `src/core/resolver.ts` — DFS-coloured topological sort with cycle detection (`CircularReferenceError` listing the cycle), missing-reference detection (`MissingReferenceError`), pure-reference (native-type-preserving) substitution, embedded-reference string interpolation, and recursive substitution into composite objects/arrays. |
| 1.5 | Build `$type` inheritance: group-level `$type` propagates to child tokens unless overridden | Core | 2h | Resolver's `collectLeaves` walks the tree carrying an inherited-type context; a group's `$type` overrides the context, a token's explicit `$type` wins over the context. Missing-type → `TokenTypeError`. |
| 1.6 | Build CSS generator: resolved tokens → `:root { --color-primary: #1a73e8; }` with deterministic key ordering and header comment | Gen | 3h | `src/generators/css.ts` — `:root` block, `--kebab-case` custom properties, `/* Generated by toki v{version} — do not edit */` header, deterministic document order. Handles color, dimension, fontFamily, fontWeight, duration, number, lineHeight, letterSpacing, `cubicBezier` (→ `cubic-bezier(...)`), `shadow` (single + multi). Multi-property composites (typography/border/transition) are skipped (deferred to Phase 2 transformers). Writes `css/tokens.css`. |
| 1.7 | Build JS generator: resolved tokens → `export const colorPrimary = "#1a73e8";` (camelCase naming) + `.d.ts` type declarations file | Gen | 2h | `src/generators/js.ts` — `export const camelCase = <literal>;` named exports + `export declare const camelCase: <type>;` declarations. Type inference (`inferJsType`) for primitives, tuples, and `readonly` object shapes with sorted keys. Reserved-word escaping (`default_`), leading-digit prefix (`_500Value`), and `GeneratorError` on identifier collisions. Writes `js/tokens.js` + `js/tokens.d.ts`. |
| 1.8 | Build CLI entry point: `toki build --input tokens.json --output ./dist --format css,js` using Commander.js | CLI | 2h | `src/cli.ts` (Commander.js), `src/core/pipeline.ts` (orchestrator), `src/utils/writer.ts` (deterministic disk writes; `--clean` only removes the platform subdirectories being written, never arbitrary user files). `src/version.ts` reads `package.json` version at runtime. Comma- or space-separated `--format`; stable `TokiError`-based error reporting with non-zero exit. Auto-invokes only as the main entry (safe to import as a library). |
| 1.9 | Write Vitest unit tests: parser (3–5 cases), resolver (refs, nested refs, circular deps, missing refs), CSS generator, JS generator | Test | 4h | 6 test files, 59 tests: `src/core/parser.test.ts` (11), `src/core/resolver.test.ts` (16), `src/generators/css.test.ts` (10), `src/generators/js.test.ts` (12), `src/core/pipeline.test.ts` (5 — end-to-end via writer with a tmp dir), `src/utils/naming.test.ts` (5). All green. |
| 1.10 | Write README.md: installation, quick start, input format reference, output examples for CSS and JS | Docs | 2h | `README.md` updated: Status → Phase 1 complete; expanded quick start with output tree and error examples; added a concrete "Output examples" section showing the generated `tokens.css`, `tokens.js`, and `tokens.d.ts` from a sample input; platform table annotated with Phase-2 markers. |

### Phase 1 Exit Criteria — all satisfied

- [x] `toki build --input tokens.json --output ./dist --format css,js` produces correct, deterministic output
- [x] All tests pass (`pnpm test`) — 59/59
- [x] Zero TypeScript errors (`pnpm typecheck`)
- [x] README has copy-paste quick start
- [x] Output files have `/* Generated by toki v{version} — do not edit */` header

## Phase 2 — Multi-Platform

**Goal:** All six target platforms plus React/Next.js generate correct, idiomatic output from the same input. ✅ **Complete.**

| # | Task | Type | Est. | Notes |
|---|---|---|---|---|
| 2.1 | Build value transformer registry: `ColorTransformer`, `DimensionTransformer`, `FontWeightTransformer`, `ShadowTransformer` | Core | 3h | `src/core/transformer.ts` — type-keyed registry (`registerTransformer`/`getTransformer`/`transformTokens`) applied as a real pipeline stage per format (`Parse → Resolve → Transform → Generate`). `ColorTransformer` normalizes hex for all platforms (shorthand expansion, lowercase). `DimensionTransformer` (`"8px"`→`8`, `"1.5rem"`→`24` @ base 16), `FontWeightTransformer` (`"bold"`→`"700"`), `ShadowTransformer` (DTCG shadow → `{ shadowColor, shadowOffset, shadowOpacity, shadowRadius, elevation }`), plus `FontFamilyTransformer` (first family name) and `TypographyTransformer` (RN composite fields) — all RN-only. |
| 2.2 | Build React Native generator | Gen | 3h | `src/generators/react-native.ts` — `tokens.js` grouped by pluralized category (`colors`, `spacing`, …) with nesting preserved + `styles.js` with `StyleSheet.create()` groups (`backgrounds`, `textColors`, `textStyles`). Shared grouping/serialization in `src/utils/grouping.ts`. |
| 2.3 | Build Angular latest generator | Gen | 3h | `src/generators/angular.ts` — `_tokens.scss` (`$kebab-case` vars), `tokens.scss` (`@use "./tokens" as tokens;` + `:root` custom properties), `tokens.ts` (`CONSTANT_CASE`), `tokens.module.ts` (`InjectionToken<DesignTokens>` + value + provider). Name-collision detection per namespace. |
| 2.4 | Build Angular 11 legacy generator | Gen | 2h | `src/generators/angular-11.ts` — reuses the Angular renderers; `tokens.scss` uses `@import "tokens";` only (no `@use`/`@forward`); no `InjectionToken` module. |
| 2.5 | Build Svelte generator | Gen | 2h | `src/generators/svelte.ts` — `tokens.css` (`:root` custom properties shared with the CSS generator via `renderCssCustomProperties`) + `tokens.ts` camelCase ES module. Works with scoped `<style>` via normal CSS-var cascade. |
| 2.6 | Build React / Next.js generator | Gen | 3h | `src/generators/react.ts` — `theme.ts` nested category object exported `as const` + `Theme` type + default export (CSS-in-JS / Tailwind `theme.extend` compatible) + companion `tokens.css` for `next-themes`. |
| 2.7 | Multi-format single run | CLI | 2h | All 7 formats registered in `src/generators/index.ts`; `--format all` expands to every format (`ALL_FORMATS_KEYWORD`); duplicate formats deduplicated; outputs under per-platform subdirectories. |
| 2.8 | Per-platform README in each output subdirectory | Docs | 2h | `src/generators/readme.ts` — every generator (all 7) emits a `README.md` artifact: quick start, naming convention, multi-theme note, known limitations. |
| 2.9 | Vitest tests for new generators + transformers | Test | 5h | 101 new tests (160 total): `transformer.test.ts` (37 — incl. seeded-PRNG property tests: any valid hex → valid RN color, px/rem → numbers, 100–900 weights, shadow invariants), 5 generator suites, `grouping.test.ts`, `readme.test.ts`. 16 snapshots (one+ per platform) in `src/**/__snapshots__`. Added `@vitest/coverage-v8@^3` dev dep; coverage 93.7% stmts / 88.1% branch / 92.3% funcs / 93.7% lines (thresholds 90/80/85/90). |

### Phase 2 Exit Criteria — all satisfied

- [x] All 7 generators (css, js, react-native, angular, angular-11, svelte, react) produce correct output
- [x] `toki build --format all` generates the complete output tree (23 artifacts for the sample set)
- [x] Angular latest output uses `@use`; Angular 11 output uses `@import` only
- [x] React Native output uses raw numbers for dimensions (not `px` strings)
- [x] React theme object is nested by category (colors, spacing, typography)
- [x] All tests pass, snapshot tests capture output regressions — 160/160
- [x] Each output subdirectory has a README